import { ToLowercaseDirective } from './to-lowercase.directive';

describe('ToLowercaseDirective', () => {
  it('should create an instance', () => {
    const directive = new ToLowercaseDirective();
    expect(directive).toBeTruthy();
  });
});
